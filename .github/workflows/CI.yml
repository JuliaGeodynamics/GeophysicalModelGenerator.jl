name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
    tags: '*'
  schedule:
    - cron: '0 15 */14 * *'

# Cancel redundant CI tests automatically
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  run_tests:
    name: Julia ${{ matrix.version }} - ${{ matrix.os }} - ${{ matrix.arch }} - ${{ github.event_name }}
    runs-on: ${{ matrix.os }}
    continue-on-error: ${{ matrix.allow_failure }}
    strategy:
      fail-fast: false
      matrix:
        version:
          - 'lts'
          - '1'
          # - 'pre'
        os:
          - ubuntu-latest
          - macos-15-intel
          - windows-latest
        arch:
          - x64
        allow_failure: [false]
        include:
          - os: macOS-latest
            arch: aarch64
            version: 'lts'
            allow_failure: false
          - os: macOS-latest
            arch: aarch64
            version: '1'
            allow_failure: false
          - os: macOS-latest
            arch: aarch64
            version: '1.12'
            allow_failure: true
          # Julia 1.13 is the current stable release; test it explicitly on Linux
          # so a regression there is visible independently of the '1' alias.
          - os: ubuntu-latest
            arch: x64
            version: '1.13'
            allow_failure: false
          # Track the upcoming release so breakage is caught early, without
          # blocking CI when an unreleased Julia is temporarily broken.
          - os: ubuntu-latest
            arch: x64
            version: 'pre'
            allow_failure: true
    steps:
      - uses: actions/checkout@v7
      - uses: julia-actions/setup-julia@v3
        with:
          version: ${{ matrix.version }}
          arch: ${{ matrix.arch }}
      - uses: julia-actions/cache@v3
        with:
          include-matrix: false
      - uses: julia-actions/julia-buildpkg@latest
      - uses: julia-actions/julia-runtest@latest
      - uses: julia-actions/julia-processcoverage@v1
      - uses: codecov/codecov-action@v7
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          fail_ci_if_error: false  # or true if you want CI to fail when Codecov fails
